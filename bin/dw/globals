#!/usr/bin/perl -w

use strict;

use FindBin qw/$Bin/;
use lib "$Bin/../../lib";

use Wikia::DW::Common;

my $globals = Wikia::DW::Common::globals;

foreach my $setting (keys %$globals) {
    printf "%s => %s\n", $setting, $globals->{$setting};
}

